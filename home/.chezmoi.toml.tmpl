{{- /* 環境タイプの決定（優先順位：環境変数 > デフォルト） */ -}}
{{- $profile := env "DOTFILES_PROFILE" -}}
{{- if not $profile -}}
{{-   $profile = "client" -}}
{{- end -}}

[data]
    # 環境タイプ: client（デフォルト） または server
    profile = "{{ $profile }}"
