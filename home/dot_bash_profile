# shellcheck shell=bash
# Login shells: everything lives in .bashrc (its top section is
# non-interactive-safe, so scp/ssh commands get PATH too).
# shellcheck disable=SC1091
[ -f "$HOME/.bashrc" ] && . "$HOME/.bashrc"
