return {
  -- In-buffer rendering (render-markdown.nvim from the markdown extra)
  -- covers reading; the browser preview needs a Node build, so it stays off.
  { "iamcco/markdown-preview.nvim", enabled = false },
}
