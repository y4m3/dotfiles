# ~/.config/bat/config — bat defaults
# See: https://github.com/sharkdp/bat#configuration

# Always use colors
--color=always

# Show line numbers and grid
--style=header,grid,numbers

# Use less as pager when needed
pager = "less -FR"
