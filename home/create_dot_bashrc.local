# Machine-local bash overrides. Created once by chezmoi, then owned by you.
# Sourced last from ~/.bashrc, so anything here wins.
#
# Example (ghq.root is ~/repos, see ~/.gitconfig):
# alias work='cd ~/repos/github.com/y4m3'
