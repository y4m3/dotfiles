# =============================================================================
# GlazeWM Configuration
# Path: ~/.glzr/glazewm/config.yaml
# =============================================================================

general:
  # Commands to run when the WM has started.
  startup_commands: []

  # Commands to run just before the WM is shutdown.
  shutdown_commands: []

  # Commands to run after the WM config is reloaded.
  config_reload_commands: []

  # Whether to automatically focus windows underneath the cursor.
  focus_follows_cursor: false

  # Whether to switch back and forth between the previously focused
  # workspace when focusing the current workspace.
  toggle_workspace_on_refocus: true

  cursor_jump:
    enabled: true
    trigger: "window_focus"

  hide_method: "cloak"
  show_all_in_taskbar: true

gaps:
  scale_with_dpi: true
  inner_gap: "6px"
  outer_gap:
    top: "6px"
    right: "6px"
    bottom: "6px"
    left: "6px"

window_effects:
  focused_window:
    border:
      enabled: true
      color: "#bb9af7"
    hide_title_bar:
      enabled: false
    corner_style:
      enabled: true
      style: "square"
    transparency:
      enabled: false
      opacity: "95%"

  other_windows:
    border:
      enabled: true
      color: "#565f89" # Tokyo Night Comment
    hide_title_bar:
      enabled: false
    corner_style:
      enabled: true
      style: "square"
    transparency:
      enabled: false
      opacity: "95%"

window_behavior:
  initial_state: "tiling"
  state_defaults:
    floating:
      centered: true
      shown_on_top: false
    fullscreen:
      maximized: false
      shown_on_top: false

workspaces:
  - name: "1"
  - name: "2"
  - name: "3"
  - name: "4"
  - name: "5"
  - name: "6"
  - name: "7"
  - name: "8"
  - name: "9"

window_rules:
  - commands: ["ignore"]
    match:
      - window_process: { equals: "1Clipboard" }
      - window_process: { equals: "AutoHotkey" }
      - window_process: { equals: "AutoHotkey64" }
      - window_process: { equals: "Heynote" }
      - window_process: { regex: "[Pp]ower[Tt]oys" }
      - window_process: { equals: "ScreenClippingHost" }
      - window_process: { equals: "TextInputHost" }
      - window_process: { equals: "Ueli" }
      - window_process: { equals: "explorer" }
      - window_process: { equals: "zebar" }

      - window_title: { regex: "[Pp]icture.in.[Pp]icture" }
        window_class: { regex: "Chrome_WidgetWin_1|MozillaDialogClass" }

# =============================================================================
# Binding Modes
# =============================================================================
binding_modes:
  # Resize mode: Alt+r to enter, Escape/Enter to exit
  - name: "resize"
    keybindings:
      - commands: ["resize --width -1%"]
        bindings: ["h", "left"]
      - commands: ["resize --width +1%"]
        bindings: ["l", "right"]
      - commands: ["resize --height +1%"]
        bindings: ["k", "up"]
      - commands: ["resize --height -1%"]
        bindings: ["j", "down"]
      - commands: ["wm-disable-binding-mode --name resize"]
        bindings: ["escape", "enter"]

# =============================================================================
# Keybindings
#
# Changes from default:
#   - Alt+a (prev-workspace) -> Alt+i
#   - Alt+s (next-workspace) -> Alt+o
#   - Alt+u/p/i/o (resize) -> REMOVED (use Alt+r mode instead)
#   - Alt+Shift+q (close) -> Alt+Shift+x
# =============================================================================
keybindings:
  # ---------------------------------------------------------------------------
  # Focus Movement: Alt + h/j/k/l or Arrow keys
  # ---------------------------------------------------------------------------
  - commands: ["focus --direction left"]
    bindings: ["alt+h", "alt+left"]
  - commands: ["focus --direction right"]
    bindings: ["alt+l", "alt+right"]
  - commands: ["focus --direction up"]
    bindings: ["alt+k", "alt+up"]
  - commands: ["focus --direction down"]
    bindings: ["alt+j", "alt+down"]

  # ---------------------------------------------------------------------------
  # Window Movement: Alt+Shift + H/J/K/L or Arrow keys
  # ---------------------------------------------------------------------------
  - commands: ["move --direction left"]
    bindings: ["alt+shift+h", "alt+shift+left"]
  - commands: ["move --direction right"]
    bindings: ["alt+shift+l", "alt+shift+right"]
  - commands: ["move --direction up"]
    bindings: ["alt+shift+k", "alt+shift+up"]
  - commands: ["move --direction down"]
    bindings: ["alt+shift+j", "alt+shift+down"]

  # ---------------------------------------------------------------------------
  # Workspace Navigation
  # CHANGED: Alt+a/s -> Alt+i/o for consistency with WezTerm/Zellij
  # ---------------------------------------------------------------------------
  - commands: ["focus --prev-active-workspace"]
    bindings: ["alt+i"]
  - commands: ["focus --next-active-workspace"]
    bindings: ["alt+o"]
  - commands: ["focus --recent-workspace"]
    bindings: ["alt+d"]

  # ---------------------------------------------------------------------------
  # Workspace Selection by Number: Alt + 1-9
  # ---------------------------------------------------------------------------
  - commands: ["focus --workspace 1"]
    bindings: ["alt+1"]
  - commands: ["focus --workspace 2"]
    bindings: ["alt+2"]
  - commands: ["focus --workspace 3"]
    bindings: ["alt+3"]
  - commands: ["focus --workspace 4"]
    bindings: ["alt+4"]
  - commands: ["focus --workspace 5"]
    bindings: ["alt+5"]
  - commands: ["focus --workspace 6"]
    bindings: ["alt+6"]
  - commands: ["focus --workspace 7"]
    bindings: ["alt+7"]
  - commands: ["focus --workspace 8"]
    bindings: ["alt+8"]
  - commands: ["focus --workspace 9"]
    bindings: ["alt+9"]

  # ---------------------------------------------------------------------------
  # Move Window to Workspace: Alt+Shift + 1-9
  # ---------------------------------------------------------------------------
  - commands: ["move --workspace 1", "focus --workspace 1"]
    bindings: ["alt+shift+1"]
  - commands: ["move --workspace 2", "focus --workspace 2"]
    bindings: ["alt+shift+2"]
  - commands: ["move --workspace 3", "focus --workspace 3"]
    bindings: ["alt+shift+3"]
  - commands: ["move --workspace 4", "focus --workspace 4"]
    bindings: ["alt+shift+4"]
  - commands: ["move --workspace 5", "focus --workspace 5"]
    bindings: ["alt+shift+5"]
  - commands: ["move --workspace 6", "focus --workspace 6"]
    bindings: ["alt+shift+6"]
  - commands: ["move --workspace 7", "focus --workspace 7"]
    bindings: ["alt+shift+7"]
  - commands: ["move --workspace 8", "focus --workspace 8"]
    bindings: ["alt+shift+8"]
  - commands: ["move --workspace 9", "focus --workspace 9"]
    bindings: ["alt+shift+9"]

  # ---------------------------------------------------------------------------
  # Move Workspace to Monitor: Alt+Shift + a/s/d/f
  # ---------------------------------------------------------------------------
  - commands: ["move-workspace --direction left"]
    bindings: ["alt+shift+a"]
  - commands: ["move-workspace --direction down"]
    bindings: ["alt+shift+s"]
  - commands: ["move-workspace --direction up"]
    bindings: ["alt+shift+d"]
  - commands: ["move-workspace --direction right"]
    bindings: ["alt+shift+f"]

  # ---------------------------------------------------------------------------
  # Resize Mode
  # REMOVED: Alt+u/p/i/o direct resize keys (use Alt+r mode instead)
  # ---------------------------------------------------------------------------
  - commands: ["wm-enable-binding-mode --name resize"]
    bindings: ["alt+r"]

  # ---------------------------------------------------------------------------
  # Window State Toggles
  # ---------------------------------------------------------------------------
  - commands: ["toggle-tiling-direction"]
    bindings: ["alt+v"]
  - commands: ["toggle-floating --centered"]
    bindings: ["alt+shift+space"]
  - commands: ["toggle-tiling"]
    bindings: ["alt+t"]
  - commands: ["toggle-fullscreen"]
    bindings: ["alt+f"]
  - commands: ["toggle-minimized"]
    bindings: ["alt+m"]
  - commands: ["wm-cycle-focus"]
    bindings: ["alt+n"]

  # ---------------------------------------------------------------------------
  # Close Window
  # CHANGED: Alt+Shift+q -> Alt+Shift+x for consistency
  # ---------------------------------------------------------------------------
  - commands: ["close"]
    bindings: ["alt+shift+x"]

  # ---------------------------------------------------------------------------
  # WM Control
  # ---------------------------------------------------------------------------
  - commands: ["wm-exit"]
    bindings: ["alt+shift+e"]
  - commands: ["wm-reload-config"]
    bindings: ["alt+shift+r"]
  - commands: ["wm-redraw"]
    bindings: ["alt+shift+w"]
  - commands: ["wm-toggle-pause"]
    bindings: ["alt+shift+p"]

  # ---------------------------------------------------------------------------
  # Launch Terminal (autohotkey recommended)
  # ---------------------------------------------------------------------------
  # - commands: ["shell-exec %ProgramFiles%/Wezterm/wezterm-gui.exe"]
  #   bindings: ["alt+enter"]
  # - commands:
  #     [
  #       "shell-exec wscript.exe %USERPROFILE%/.config/scripts/toggle-wezterm.vbs",
  #     ]
  #   bindings: ["alt+enter"]
