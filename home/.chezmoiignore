# Patterns match TARGET paths (attributes like dot_/run_/.tmpl already stripped).
# Script gating lives in each script's template guard, not here.

{{ if ne .chezmoi.os "windows" }}
# Windows-only targets
Documents/
{{ end }}

{{ if eq .chezmoi.os "windows" }}
# Linux-only targets. nvim/fd/rg configs stay here too: the minimal Windows
# layer ships no nvim, and nothing on Windows sets RIPGREP_CONFIG_PATH or
# XDG paths, so deploying them would only create dead files.
.bashrc
.bash_profile
.bashrc.d/
.config/fd/
.config/herdr/
.config/nix/
.config/nvim/
.config/tmux/
.local/bin/
.ripgreprc
{{ end }}
