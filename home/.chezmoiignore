# Patterns match TARGET paths (attributes like dot_/run_/.tmpl already stripped).
# Script gating lives in each script's template guard, not here.

{{ if ne .chezmoi.os "windows" }}
# Windows-only targets
Documents/
{{ end }}

{{ if eq .chezmoi.os "windows" }}
# Linux-only targets. fd/rg configs stay here too. Windows ships the nvim
# config: run_once_110-windows-xdg-env.ps1.tmpl persists XDG_CONFIG_HOME so
# nvim finds it. Nothing on Windows sets RIPGREP_CONFIG_PATH, so that config
# would only create an unused file.
.bashrc
.bash_profile
.bashrc.d/
.config/fd/
.config/herdr/
.config/nix/
.config/tmux/
.local/bin/
.ripgreprc
{{ end }}
