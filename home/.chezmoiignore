# Patterns match TARGET paths (attributes like dot_/run_/.tmpl already stripped).
# Script gating lives in each script's template guard, not here.

{{ if ne .chezmoi.os "windows" }}
# Windows-only targets
Documents/
{{ end }}

{{ if eq .chezmoi.os "windows" }}
# Linux-only targets. nvim/fd/rg configs stay here too. The minimal
# Windows layer ships no nvim. Nothing on Windows sets RIPGREP_CONFIG_PATH
# or XDG paths. So deploying them would only create unused files.
.bashrc
.bash_profile
.bashrc.d/
.config/fd/
.config/herdr/
.config/nix/
.config/nvim/
.config/tmux/
.local/bin/
.ripgreprc
{{ end }}
